# AG-02 fixtures

- `request.json` is the canonical workflow input sample.
- `workflow_result.json` is the approved end-state sample.
- `minimal_warning_result.json` is the warning end-state sample when RAG is insufficient.
